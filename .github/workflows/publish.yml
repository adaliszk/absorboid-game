name: "Publish"

on:
  workflow_dispatch: {}
  pull_request: {}
  push:
    branches:
      - release
      - develop

jobs:

  build:
    name: "Build"
    strategy:
      matrix:
        export: [web, windows, mac]
        include:
          - export: web
            output: index.html
            os: ubuntu-latest
          - export: windows
            output: game.exe
            os: windows-latest
          - export: mac
            output: game.dmg
            os: macos-latest
    runs-on: ${{ matrix.os}}
    steps:
      - name: 🧾 Checkout
        uses: actions/checkout@v3
        with:
          lfs: true
      -
        name: 🤖 Setup Godot
        uses: chickensoft-games/setup-godot@v1
        with:
          version: 4.1.1
      -
        name: 🤖 Setup Project
        run: |
          echo "Build#${{ github.run_number }}" > git_version.txt
      -
        name: 🔬 Verify Setup
        run: |
          godot --version
          cat git_version.txt
      -
        name: 🦺 Build Export
        run: |
          godot --headless --verbose --export-release "${{ matrix.export }}" ./build/${{ matrix.export }}/${{ matrix.output }}
      -
        name: 🔬 Verify Files
        run: |
          tree ./build
      -
        name: 📁 Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.export }}-v${{ github.run_number }}
          path: ./build/${{ matrix.export }}/*
